apiVersion: v1
kind: Service
metadata:
  name: ro-visualisation
spec:
  selector:
    app: ro-visualisation
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30010
  type: NodePort
